{
  "name": "@open-agent-harness/agent-daemon",
  "version": "0.1.0",
  "description": "Per-machine daemon — wraps OpenCode CLI, connects to state-server WebSocket channel",
  "type": "module",
  "bin": {
    "agent-daemon": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.39.0"
  },
  "devDependencies": {
    "bun-types": "^1.3.10"
  }
}
