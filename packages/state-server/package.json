{
  "name": "@open-agent-harness/state-server",
  "version": "0.1.0",
  "description": "Distributed state server — replaces file-based state/mailbox with WebSocket channel",
  "type": "module",
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "bun-types": "^1.3.10"
  }
}
